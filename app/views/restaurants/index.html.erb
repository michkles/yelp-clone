<% if @restaurants.all.any? %>
  <% @restaurants.all.each do |restaurant| %>
    <h3><%= link_to restaurant.name, restaurant_path(restaurant)%></h3>
  <% end %>
<% else %>
  <h2> NOOO RESTAURANTS EVER!!! </h2>
<% end %>



<a href="<%= new_restaurant_url %>">Add a restaurant</a>
